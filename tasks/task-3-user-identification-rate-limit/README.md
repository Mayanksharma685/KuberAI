# Task 3 — User Identification + Rate Limiting
Reserved structure. Implement later:
- localStorage UUID
- IP capture
- hash(IP + localToken)
- max 5 messages per agent per server day
- 429 RATE_LIMITED response
